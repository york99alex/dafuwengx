import { KeyValues } from "../kv";

export class CardFactory {
    
    static create(cardType:number, nPlayerID:number) {
        // 获取卡牌信息
        if(KeyValues.CardKV){
            
        }


    }
}